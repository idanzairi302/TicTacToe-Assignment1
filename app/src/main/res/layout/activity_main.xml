<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Player X's Turn"
        android:textSize="24sp"
        android:layout_marginBottom="20dp"/>

    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:rowCount="3"
        android:columnCount="3">

        <!-- Row 0 -->
        <Button android:id="@+id/btn0" style="@style/TicTacButton"/>
        <Button android:id="@+id/btn1" style="@style/TicTacButton"/>
        <Button android:id="@+id/btn2" style="@style/TicTacButton"/>

        <!-- Row 1 -->
        <Button android:id="@+id/btn3" style="@style/TicTacButton"/>
        <Button android:id="@+id/btn4" style="@style/TicTacButton"/>
        <Button android:id="@+id/btn5" style="@style/TicTacButton"/>

        <!-- Row 2 -->
        <Button android:id="@+id/btn6" style="@style/TicTacButton"/>
        <Button android:id="@+id/btn7" style="@style/TicTacButton"/>
        <Button android:id="@+id/btn8" style="@style/TicTacButton"/>
    </GridLayout>

    <Button
        android:id="@+id/btnReset"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reset Game"
        android:layout_marginTop="20dp"
        android:visibility="gone"/>

</LinearLayout>
s